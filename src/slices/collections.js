import {
  staticAssets
} from './static';
import {
  metadata
} from './metadata';
import {
  searchReports
} from './search-reports';
import {
  config
} from './config';

export const collectionsReducers = {
  staticAssets,
  metadata,
  config,
  searchReports
};
